# Пульс клиента — сводка проекта

Документ фиксирует контекст, принятые решения и текущее состояние фронтенда MVP сервиса **«Пульс клиента»** для встраивания в карточку клиента **SFA** (Альфа-Банк, сегмент СКБ).

---

## 1. Источники и цель

| Источник | Содержание |
|----------|------------|
| `БТ_Пульс_FINAL1.docx` | Бизнес-требования: MVP, методология индекса, блоки VoC / ClaimCRM / AI-рекомендации |
| `Проект «пульс клиента» 1.xlsx` | 21 клиент: PIN, индекс, статус, VoC, претензии, рекомендации для КМ |
| Референс интерфейса (Кардиомедикс) | Актуальный макет карточки клиента SFA: шапка, статус-плашки, вкладки, нижние разделы |

**Цель MVP:** дать клиентскому менеджеру «карточку готовности к разговору» — индекс настроения, обратную связь, претензии и AI-рекомендации без перехода в другие системы.

**Бэкенд не реализован** — все данные статические (mock из Excel).

---

## 2. Технологический стек

- **React 19** + **TypeScript**
- **Vite 6** — сборка
- **React Router 7** (`HashRouter` — для автономного HTML без сервера)
- **CSS Modules** — стили в духе SFA / Альфа-Банк
- Тема переключается в `pulse-client/src/config/theme.ts` (`sfa` | `default`)

---

## 3. Структура репозитория

```
work_ui/
├── БТ_Пульс_FINAL1.docx          # исходные БТ
├── Проект «пульс клиента» 1.xlsx # исходные данные
├── Пульс клиента — демо.html     # автономный файл для заказчика (без сервера)
├── Собрать демо для заказчика.bat
└── pulse-client/                 # исходный код
    ├── src/
    │   ├── config/               # theme, routes, default client
    │   ├── data/clients.ts       # 21 клиент (mock)
    │   ├── types/                # PulseZone, ClientPulse
    │   ├── components/
    │   │   ├── Layout/           # LayoutSfa, LayoutDefault
    │   │   ├── SfaClientChrome/  # шапка карточки по референсу
    │   │   ├── ClientSidebar/    # поиск, сортировка, пагинация
    │   │   ├── PulseWidget/      # основной виджет MVP
    │   │   └── PulseBadge/       # (legacy) круговой индикатор
    │   └── pages/
    ├── scripts/build-demo.mjs    # сборка single-file HTML
    └── demo/                     # промежуточная сборка
```

---

## 4. Интерфейс по референсу (карточка клиента SFA)

Макет приведён к скриншоту карточки **Кардиомедикс**, с подстановкой **наших данных**.

### 4.1. Верхняя зона

| Элемент | Реализация |
|---------|------------|
| Хлебные крошки | Панель › Поиск клиентов › {название компании} |
| Название ЮЛ | CAPS, полное наименование из mock |
| Теги | «Торговая организация», «Клиент СКБ» (декоративно) |
| Мета-строка | КП/PIN, город, ИНН |

### 4.2. Ряд статус-плашек (ключевое обновление)

Горизонтальный ряд под шапкой — как на референсе:

| Плашка | Источник данных |
|--------|-----------------|
| **Предложение** | Число продуктов из `suggestedProducts` |
| **Претензии** | 1, если в `claimSummary` есть претензия |
| **ЗП клиент** | Статус ✓ (зелёный) |
| **Есть в БДК** | Статус ✓ |
| **Клиент** | Статус ✓ |
| **ВЭД (не склонен)** | Статус ✓ |
| **Задолженность** | Серый статус |
| **Пульс клиента** | Индекс в цветном круге (зона светофора); **клик → виджет MVP** |

Логика плашек: `src/components/SfaClientChrome/statusBadges.ts`

### 4.3. Вкладки карточки

**Основные** (как в SFA): Основное · Бизнес · ЕГРЮЛ · СПАРК · Банк · Контакты · Адреса · Контактные лица

- **Основное** — сетка полей + блок «Комментарии» / «Полезные ссылки» (`SfaMainTabContent`)
- Остальные — заглушка «не входит в MVP»

**Нижние** (декоративно): Задачи · Операции · Портфель · Продукты · Лиды · …

### 4.4. Контент MVP

Открывается по клику на плашку **«Пульс клиента»** (не отдельная вкладка в верхнем ряду):

- Интерпретация индекса
- VoC: положительные / негативные стороны
- Претензии (ClaimCRM)
- AI-рекомендация для КМ + продуктовые теги
- Рекомендации по лояльности (по зоне светофора)

### 4.5. Боковая панель «Клиенты»

- Поиск по PIN / названию / ИНН
- Сортировка: выпадающий список (Название · PIN · **Пульс**) + ↑/↓
- **По умолчанию:** Пульс ↓ (убывание)
- **Пагинация:** 10 клиентов на страницу, переключатель ← 1/3 →

### 4.6. Левая навигация SFA

Узкая панель: Задачи · **Клиенты** · Сделки · Отчёты · Настройки

---

## 5. Данные

- **21 клиент** из Excel (`src/data/clients.ts`)
- **Стартовый клиент (демо HTML):** `UCE8O9` — **ООО «АльфаЛогистик»** (`src/config/routes.ts` → `DEFAULT_CLIENT_PIN`)
- Пример из БТ: `UAA7IL` — ООО «ТехноСервис» (индекс 2.6, критик)

### Зоны светофора

| Индекс | Зона | Цвет |
|--------|------|------|
| 1–3 | Критик | Красный |
| 3.01–4.99 | Нейтральный | Жёлтый |
| 5 | Лояльный | Зелёный |
| Нет данных | Серая зона | Серый |

### Формула (из БТ, блок 1)

```
Пульс = 5 − балл_претензии − балл_VoC_продукт − балл_VoC_канал − балл_VoC_сервис
```

Источники MVP: **ClaimCRM**, **VoC-платформа**. Блоки 2–3 (продуктовые рекомендации АППП, лояльность по сегменту) — в UI частично, расчёт не автоматизирован.

---

## 6. Темы оформления

| Тема | Описание | Откат |
|------|----------|-------|
| `sfa` | Встраивание в SFA (текущая) | `APP_THEME = 'default'` в `theme.ts` |
| `default` | Standalone: тёмный хедер, карточки | — |

CSS-переменные SFA: `src/styles/themes/sfa.css`

---

## 7. Сборка и демо для заказчика

### Разработка

```bash
cd pulse-client
npm install
npm run dev          # http://localhost:5173
```

### Production

```bash
npm run build
```

### Автономный HTML (без сервера)

```bash
npm run build:demo
# → work_ui/Пульс клиента — демо.html
```

Или: двойной клик **`Собрать демо для заказчика.bat`**

- Один файл ~350 КБ, открывается в Chrome/Edge
- Hash-навигация: `#/client/UCE8O9` (АльфаЛогистик)
- При первом открытии hash выставляется скриптом в HTML

---

## 8. Ключевые файлы конфигурации

| Файл | Назначение |
|------|------------|
| `src/config/theme.ts` | `sfa` / `default` |
| `src/config/routes.ts` | `DEFAULT_CLIENT_PIN = 'UCE8O9'`, пути |
| `src/config/routes.ts` → `defaultClientPath()` | Стартовый маршрут приложения |
| `scripts/build-demo.mjs` | Inline CSS+JS, IIFE, default hash |

---

## 9. Что входит / не входит в MVP

### Входит

- [x] Индикатор «Пульс» (светофор + число)
- [x] Плашка «Пульс клиента» в ряду статусов SFA
- [x] VoC: позитив / негатив
- [x] Претензии ClaimCRM
- [x] AI-рекомендация для КМ
- [x] Рекомендации по лояльности
- [x] Поиск, сортировка, пагинация (10/стр.)
- [x] 21 клиент из Excel
- [x] Демо HTML для заказчика
- [x] Карточка SFA по референсу (шапка, плашки, вкладки)

### Не входит (NextStep / вне скоупа)

- [ ] Реальный бэкенд, BI, SFA API
- [ ] Блок 2: продуктовые рекомендации АППП (жёлтые флаги)
- [ ] Блок 3: сценарии лояльности по сегменту (автоматический расчёт)
- [ ] Чат КЦ, NPS ЛПР, Альфа Босс
- [ ] Нижние вкладки (Задачи, Сделки…) — только UI-заглушка
- [ ] Human feedback по AI-рекомендациям

---

## 10. Хронология решений (кратко)

1. Создан фронт на React+Vite по БТ и Excel
2. Дизайн Альфа-Банк: красный акцент, SFA-layout
3. Тема `sfa` вынесена отдельно для лёгкого отката
4. Интерактивность: навигация, поиск, вкладки
5. Увеличен индикатор «Пульс» (круг 140px в шапке → затем перенесён в плашку)
6. Автономный `Пульс клиента — демо.html` (file://)
7. Пагинация 10 клиентов, сортировка по пульсу ↓
8. Стартовый клиент: АльфаЛогистик (`UCE8O9`)
9. Интерфейс приведён к референсу Кардиомедикс + плашка «Пульс клиента»

---

## 11. Критерии успеха (из БТ, для пилота)

- Снижение оттока, NPS, attach rate — оценка после пилота
- DAU/WAU КМ ≥ 50%
- Precision светофора ≥ 80%, полезность AI-рекомендаций ≥ 75%

---

## 12. Контакты и системы-источники (из БТ)

| Система | Назначение в MVP |
|---------|------------------|
| VoC-платформа | Оценки, комментарии |
| Claim CRM | Претензии |
| SFA | Хост виджета |
| АППП | NextStep — кошелёк клиента |

---

*Последнее обновление: июнь 2026. При изменении UI или данных — пересобрать демо: `npm run build:demo`.*
